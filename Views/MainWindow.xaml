<Window x:Class="WalkyDog.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WalkyDog"
        mc:Ignorable="d"
        Title="MainWindow" Height="560" Width="500">
    <Grid>
        <Frame Source="/Views/Login.xaml" Background="#FFF7B568" Foreground="#FFF97D37"/>

    </Grid>
    <!--
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0*"/>
            <ColumnDefinition Width="0*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>


        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Grid.ColumnSpan="3" Margin="0,0,-0.4,0">
            <Label>User Name:</Label>
            <TextBox Text="{Binding User.Name, UpdateSourceTrigger=PropertyChanged}" Width="136"/>
            <Button Command="{Binding UpdateCommand}">Update</Button>
        </StackPanel>
        -->

</Window>
